# ModernArt 项目协作规则

## 项目目标

- 构建一个准确、可浏览、可持续维护的现代艺术史互动网站。
- 内容准确性、作品图像与元数据的可追溯性优先于页面数量。

## 目录约定

- `src/data/`：艺术史内容、关系与 TypeScript 数据结构。
- `src/components/`：可复用界面组件；按页面或领域分组。
- `src/pages/`：路由级页面。
- `public/artworks/`：已核验并用于网站展示的作品图片。
- `docs/`：需求、审计台账、维护说明与阶段记录。
- `scripts/`：内容校验、资源获取等可重复执行的维护脚本。

## 内容与图片规则

- 每件作品必须有标题、艺术家、年份和所属流派；在引入独立 ID 前，以 `movement id + title + artist` 作为台账中的稳定身份。
- 图片允许缺省，但必须在 `docs/CONTENT_AUDIT.md` 标明是待补、版权受限占位，还是存疑待核验。
- 非营利、科普、无广告或标注来源都不自动构成图片使用许可；正式展示图仍须有公有领域、明确开放许可、馆方书面许可或经记录的法定例外依据。
- 一张现场摄影的 CC 许可通常只覆盖摄影者拥有的权利；若画作、装置或表演仍在版权期，必须另外核对底层作品权利。
- 本项目的主要发布法域与主要受众确定为中国大陆（2026-08-28）。公有领域判断按中国大陆规则记录；仅在中国大陆保护期届满的图片必须标记 `PD-China`，并在网站图片说明中保留该法域提示。
- `PD-China` 只解决底层艺术品的作品权利。近期扫描、现场摄影、装置照或表演影像仍须另有公版、开放许可、馆方教育许可或可记录的法定依据。
- 新增或替换图片前，核对作品标题、作者、年份及来源；来源和授权状态应可追溯。
- 不用仅凭文件名或视觉相似度判断图片正确性。
- 缺失、存疑、待核验项目统一记录在 `docs/CONTENT_AUDIT.md`，处理后更新状态与日期。
- 图片文件使用小写英文 kebab-case；同一作品只保留一个正式展示版本。
- 无图作品的馆藏页、候选文件页与法域判断统一维护在 `docs/IMAGE_SOURCE_CANDIDATES.md`；候选链接不等于正式授权。

## 变更纪律

- 先更新规则或数据结构，再按新规则修改内容与页面。
- 不修改 `.env`、密钥、CI/CD、数据库结构，不执行发布、推送或破坏性 Git 操作，除非得到明确授权。
- 不删除文件；发现废弃资源时先在台账中标记。
- 保留用户已有改动，不处理与当前任务无关的问题。

## 响应式与无障碍约定

- `1024px` 以下使用线性时间浏览，不把桌面空间星图等比缩小；`1024px` 及以上保留可拖拽星图。该断点按关系图的可读性确定，不按设备名称确定。
- 同一操作只保留一个可聚焦的语义入口；优先使用原生 `button`、`a`、`input`，不在普通容器上模拟重复按钮。
- 所有仅依赖拖拽、滚轮或 hover 的能力都必须提供键盘或线性浏览的等价路径。
- 响应式改动至少验收 `390px`、`768px`、`1280px` 三档，并检查键盘焦点和 `prefers-reduced-motion`。
- 本地三档验收可使用 `scripts/responsive-harness.html`；它只用于开发预览，不进入生产构建。

## 验证

- 图片变更后运行 `npm run verify:art`；它检查文件、尺寸、哈希、重复图、孤儿图及溯源字段。
- 发布前运行 `npm run verify:content`；该严格模式会把 `needs-review` 历史图片也视为未通过。
- 自动校验不能替代作品身份与许可的人工判断；人工核验来源仍需同步到 `docs/CONTENT_AUDIT.md` 和 `src/data/artworkImageMeta.ts`。
- 代码变更后运行 `npm run build`。
- 若项目新增测试或检查命令，应同步更新本文件。
